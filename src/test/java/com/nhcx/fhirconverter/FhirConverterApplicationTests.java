package com.nhcx.fhirconverter;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic test to verify the Spring application context loads correctly.
 * Uses H2 in-memory database (no MySQL needed for tests).
 */
@SpringBootTest
class FhirConverterApplicationTests {

    @Test
    void contextLoads() {
        // If this test passes, it means:
        // - All Spring components are correctly configured
        // - All dependencies are properly injected
        // - The application can start up without errors
    }
}
